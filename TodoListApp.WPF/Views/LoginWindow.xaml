<Window x:Class="TodoListApp.WPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="TODO List - Вход" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,20,0,40">
            <TextBlock Text="TODO List" 
                       FontSize="32" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            <TextBlock Text="Система управления задачами" 
                       FontSize="14" 
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Login Form -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBox materialDesign:HintAssist.Hint="Имя пользователя"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,20"
                     FontSize="14"/>

            <PasswordBox x:Name="PasswordBox"
                         materialDesign:HintAssist.Hint="Пароль"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         Margin="0,0,0,20"
                         FontSize="14"
                         PasswordChanged="PasswordBox_PasswordChanged"/>

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="Red"
                       TextWrapping="Wrap"
                       Margin="0,0,0,10"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

            <Button Content="ВОЙТИ"
                    Command="{Binding LoginCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Height="45"
                    FontSize="14"
                    Margin="0,10,0,15"/>

            <Button Content="Регистрация"
                    Command="{Binding RegisterCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Footer -->
        <TextBlock Grid.Row="2" 
                   Text="© 2026 TODO List App" 
                   HorizontalAlignment="Center"
                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                   FontSize="12"
                   Margin="0,20,0,10"/>
    </Grid>
</Window>
